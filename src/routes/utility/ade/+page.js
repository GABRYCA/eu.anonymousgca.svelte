export const load = async ({}) => {
    return {
        title: 'ADE - Calcolatore Bit',
        description: 'Bit memoria e cache, calcolatore e convertitore bit a bytes per architettura degli elaboratori (Insubria).',
        keywords: 'ade, calcolatore, bit, memoria, cache, convertitore, bytes, byte, architettura, elaboratori, insubria',
    }
}